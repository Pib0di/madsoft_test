name: madsoft_test
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  retrofit: ^4.1.0
  dio: ^5.4.3+1
  bloc: ^8.1.4
  json_annotation: ^4.9.0
  flutter_svg: ^2.0.10+1
  flutter_bloc: ^8.1.5
  device_info_plus: ^10.1.0
  path_provider: ^2.1.3
  external_path: ^1.0.3
  google_fonts: ^6.2.1
  system_info: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
  retrofit_generator: ^8.1.0
  build_runner: ^2.4.9
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/icons/
    - assets/images/
    - assets/images/mock_scheme.png
